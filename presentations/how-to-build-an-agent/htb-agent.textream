["第一页\n各位同事大家好，今天我分享的主题是\"如何构建一个智能体\"。\n\n先聊聊为什么选这个话题。过去一年我观察到就业市场正在发生一个明显的变化：传统开发岗位，包括我们后端，需求在收缩，而 Agent 开发相关的岗位在快速增长。\n\n但这里面有个现象值得注意：一些传统岗位被重新包装成了\"Agent 开发工程师\"，挂个新名字，实际面试和进去之后干的还是 Java 业务。这个趋势反而说明，Agent 已经不是某个小众方向，它火到 HR 要拿它当关键词来招人了。\n\n比我大一届的学长，最近参加暑期实习校招，碰到了几种以前没见过的面试方式。一种是面试官直接给一个 Agent 和一个 API Key，要求当场按他的需求搭出一个能跑的系统。另一种是给一道比较复杂的逻辑题，配一个没有深度思考能力的裸模型，就是那种即时输出、不经过推理链的小模型。然后要求你通过对话和 prompt 引导，让这个模型把问题解出来。\n\n同时他还反馈，编程语言基础、数据结构、计算机网络这些传统八股被问到的次数明显变少了，有的面试甚至连算法题都不做了。面试的考察重心正在从\"你会不会写代码\"转向\"你能否驾驭 AI 来解决问题\"。\n\n我们的判断是，互联网开发的就业市场，包括面试方式、岗位职责、人头数量，正在进行进化，这种进化是波及整个行业结构的。今年这种进化刚刚开始，行业需要一个试探期和一个适应期，两三年之后可能又会回到我们以前那种比较稳定的状态。所以今天我想从一个非常具体的切入点，ReAct 模式，带大家理解 Agent 到底是怎么构建的，它的核心机制是什么。\n\n","第二页\n\n大家看这个图，屏幕上是今天第一页正式内容。中间是一个对话界面，四个角各挂了一个能力模块。这就是我对 Agent 的基本拆解。\n\n先聊一个概念问题。一个原生 LLM——就是没包装过的、纯文本进文本出的模型——它是不是 Agent？\n\n我的看法是：不是。一个原生 LLM 只会做一件事：你给一段文本，它续写一段文本。它不会自己决定\"我该查一下数据库\"，不会在给出答案前先反问你确认信息，更不会记住五分钟前的对话做上下文衔接。这些能力是后来加上去的。\n\n但到底要加多少才算 Agent？这个边界在业界没有统一答案。有人说只要有 Tool Call 就算，有人说还得有 Planning，有人说必须有 Memory 才算完整。我今天采用一个比较通用的分类方法：Perception、Reasoning、Action、Memory——四个能力模块。任何一个 Agent 框架几乎都能映射到这个四象限里。今天我想讲述的是 Reasoning 和 Action，以及它们之间的关系。Reasoning 负责判断\"该做什么\"，Action 负责\"去做\"。至少拥有这两点才能算一个 agent。\n\n这四个模块听起来直观，但实际用的时候容易模糊。Perception 和 Action之间有一个很自然的问题——\"当我调用了一个工具，自然要读取工具返回的结果，这个读取过程算不算 Perception？\" 其实这不能算。Perception 指的是进入 Agent 循环之前的信息预处理——用户的原始输入、文件上传、系统状态的初始读取。而\"拿到工具结果 → 把结果放进下一轮上下文\"这个过程，属于 Action 模块的回传链路。换句话说，Perception 管的是\"进入循环前把世界看清楚\"，Action 管的是\"动手之后把结果接回来\"。两者的边界就是 Agent 循环的入口。\n\n我刚才提到原生 LLM 没有记忆，有朋友可能就要问了：2022 年我就体验过 ChatGPT 了，那时候的 3.5 已经能记住我前一句话说的\"叫我小王\"、后面就一直叫我小王了——你为什么说它没记忆？其实那个\"记住\"的本质很简单：网页后端把你的历史对话全部 append 到 messages 数组里，一次性塞给模型。模型看到的是一整串历史记录，所以它当然能\"记住\"。但这不是 Memory 技术，这只能叫上下文累积，在很早的 NLP 技术里就有了。\n\n它和真正的 Memory 差在哪？当你聊到第 16 轮，前面 4096 tokens 窗口撑满了，后端就会直接从第 1 轮开始丢消息。模型突然不知道你是谁了，因为那条消息已经从数组里被删了。所以上下文累积解决的是\"短对话内怎么让模型看到历史\"，而 Memory 解决的是\"窗口有限的情况下怎么选择性保留最重要的信息\" Memory 模块真正在做的是 摘要、向量检索、分层缓存。\n回到这张图\n\n大家再看一眼这四个模块和中间的聊天界面。我想表达的是：这四个能力不是四个独立的系统，它们最终汇聚到同一个地方——上下文。下一张图我会具体展开这一点。\n\n好，这一页先到这里。","第三页\n\n上一页我们拆了四个能力模块，但如果你以为它们对应四个子系统或者四个微服务，那就想复杂了。真相其实很朴素：Perception 整理的外部信息、Memory 筛选的历史记录、系统规则、可用工具的清单，全都在同一次 LLM 请求的 messages 数组里。大家看这张图，左侧是一份上下文文稿，从上到下依次是系统 prompt、工具描述、历史记忆，黄色高亮那一行才是用户输入。注意看比例，用户输入只是这份文稿里很小的一段，周围全是工程层塞进去的东西。\n\nAgent 到底做了什么事？主要是组装上下文、调用 LLM、解析模型返回的动作指令、执行对应的工具函数、把返回值写回下一轮上下文——就这五件事，循环做。图中 Agent 把文稿转给右侧的 LLM，LLM 返回判断结果，Agent 再根据判断去执行。Agent 没有改写模型内部的运行方式，模型还是那个模型，变的是每次喂给它的上下文里包含了什么。\n\n所以说模型的这些能力，在工程实践上实质就是这么一个文档拼接。下一章我们看 ReAct 怎么让 Reasoning 和 Action 在这份文稿上跑起来。","第四页\n\n从上一章的四个能力模块我们聚焦到了 Reasoning 加 Action，这个方向的历史起点就在 2022 年。ReAct 由姚顺雨等七位作者联合发布，Princeton 和 Google Research 的合作产物。\n\n论文解决的是一对矛盾。左边 Chain-of-Thought 让模型把推理过程写出来，多想几步，但模型闭门造车，推理链条错了也不知道，幻觉一路滚雪球。右边 Tool Use 让模型调用外部工具，动手去查，但缺乏推理过程，模型不知道什么时候该查、查完怎么分析。ReAct 的方案是把二者拧成交替循环：先想一步，再调工具，看到结果再想下一步。推理靠外部信息纠偏，行动靠推理指引方向。前面代码里的 for 循环每一轮做的就是这件事。\n\n最后看历史地位。LangChain Agent、AutoGPT、OpenAI 的 Function Calling、Claude 的 Tool Use，所有这些后来者本质上都是 ReAct 论文的工程化实现。没有这篇 2022 年的论文，就没有现代 Agent 这个方向。好，下一张我们具体看这个循环怎么跑。","第五页","第六页\n\n当然，Agent 的工程实现还有一些更底层的细节我这次没有准备。比如一个 turn 什么时候算结束、需不需要一个状态机来管理 Agent 当前处于思考还是执行的阶段、多个工具调用是串行还是并发、token 预算怎么分配、流式输出怎么和循环配合——这些问题在实际落地时确实绕不开。但它们更多属于工程选型层面，不同框架、不同场景下的取舍不一样，没有统一的标准答案。今天我想先帮大家建立一个清晰的心智模型：四个能力模块加上 ReAct 循环，这两个概念抓住了，后面无论看到什么 Agent 框架，都能快速理解它在做什么、为什么这么设计。底层细节如果大家有兴趣，我们可以下次单独安排一次来深入。好，接下来看一下实际效果。","第七页\n\n"]